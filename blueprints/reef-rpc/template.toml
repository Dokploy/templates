[variables]
NODE_NAME = "${node_name}"
P2P_PORT = ${p2p_port}
RPC_PORT = ${rpc_port}
CHAIN_SPEC_URL = "${chain_spec_url}"
BOOTNODE_ADDR = "${bootnode_addr}"

[[config.domains]]
serviceName = "reef-rpc"
port = ${RPC_PORT}
host = "localhost"
path = "/"

[config.env]
NODE_NAME = "${NODE_NAME}"
P2P_PORT = "${P2P_PORT}"
RPC_PORT = "${RPC_PORT}"
CHAIN_SPEC_URL = "${CHAIN_SPEC_URL}"
BOOTNODE_ADDR = "${BOOTNODE_ADDR}"
