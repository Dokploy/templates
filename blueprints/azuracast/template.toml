[variables]
main_domain = "${domain}"
admin_password = "${password:16}"

[config]
[[config.domains]]
serviceName = "azuracast"
port = 8080
host = "${main_domain}"

[config.env]
LANG = "en_US.UTF-8"
AZURACAST_DC_REVISION = "1"

[[config.mounts]]