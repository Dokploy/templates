version: "3.8"

services:
  nats:
    image: nats:2.10.24-alpine
    restart: "no"
    expose:
      - 4222
      - 8222
      - 8080
    command: ["-c", "/etc/nats/nats.conf"]
    volumes:
      - ../files/nats.conf:/etc/nats/nats.conf
      - nats-data:/data

volumes:
  nats-data:
