services:
  jellyseerr:
    image: fallenbagel/jellyseerr:latest
    restart: unless-stopped
    volumes:
      - jellyseerr_config:/app/config
    environment:
      LOG_LEVEL: info
    ports:
      - "5055"

volumes:
  jellyseerr_config:
