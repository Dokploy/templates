[variables]
main_domain = "${domain}"
clickhouse_password = "${password:32}"

[config]
env = [
    "CLICKHOUSE_PASSWORD=${clickhouse_password}"
]

[[config.domains]]
serviceName = "frontend"
port = 8080
host = "${main_domain}"