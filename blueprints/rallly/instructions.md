# Rallly

Rallly requires SMTP to send sign-in links and invitations. After importing the template, set `SMTP_HOST`, `SMTP_USER`, and `SMTP_PWD` for your mail provider before using it with real users.

