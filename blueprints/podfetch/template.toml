[variables]
main_domain = "${domain}"
admin_password = "${password:24}"

[config]
env = [
  "USERNAME=admin",
  "PASSWORD=${admin_password}"
]

[[config.domains]]
serviceName = "podfetch"
port = 8000
host = "${main_domain}"
